[project]
name = "landmark-experiments"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
    "torchvision>=0.15",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "torchmetrics[image]",
]
